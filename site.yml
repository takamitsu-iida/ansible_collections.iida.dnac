---

#
# Cisco DNA Center REST API
#

# Takamitsu IIDA (@takamitsu-iida)

- name: access to cisco dna center rest api
  hosts: sandboxdnac2
  gather_facts: False

  tasks:
    - name: get auth token
      # delegate_to: bastion
      iida.dnac.token:
      register: r

    - debug:
        var: r
